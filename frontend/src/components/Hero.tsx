export function Hero() {
  return (
    <div className="mx-auto mb-9 mt-2 max-w-xl text-center">
      <h1 className="mb-2.5 text-[clamp(24px,3vw,32px)] font-semibold tracking-tight text-text">
        Fact-check live audio as it happens
      </h1>
      <p className="text-[15px] leading-relaxed text-text-dim">
        Capture a stream's audio, transcribe it in real time, and get
        instant claim-by-claim evaluation backed by sourced evidence.
      </p>
    </div>
  );
}
